@page "/pagination"

<PageTitle Title="分页(BootPagination)">
    分页是最常见的数据组件之一，用户使用其他数据组件来呈现数据，并结合分页组件来使用。
</PageTitle>
<PresentationPart Title="简单示例">
    <Description>
        以100条数据为例。使用 <code>CurrentPage</code> 表示当前页码，<code>PageSize</code> 表示每一页的数据量。
    </Description>
    <RunTemplate>
        <BootPagination TotalCount="100" />        
        <BootPagination TotalCount="100" CurrentPage="3" />
        <BootPagination TotalCount="100" CurrentPage="8" PageSize="5" />
    </RunTemplate>
    <CodeTemplate>
        @Code.GetCode(@"
```html
<BootPagination TotalCount=""100"" />        
<BootPagination TotalCount=""100"" CurrentPage=""3"" />
<BootPagination TotalCount=""100"" CurrentPage=""8"" PageSize=""5"" />
```
")
    </CodeTemplate>
</PresentationPart>

<PresentationPart Title="自定义分页文字">
    <Description>
        使用 <code>FirstPageText</code>：首页、 <code>PreviousPageText</code>：上一页、<code>NextPageText</code>：下一页、<code>LastPageText</code>：末页来自定义显示的文字。
    </Description>
    <RunTemplate>
        <BootPagination FirstPageText="<" LastPageText=">" TotalCount="100" />
        <BootPagination FirstPageText="|<" PreviousPageText="<<" NextPageText=">>" LastPageText=">|" CurrentPage="10" TotalCount="100" />
    </RunTemplate>
    <CodeTemplate>
        @Code.GetCode(@"
```html
<BootPagination FirstPageText=""<"" LastPageText="">"" TotalCount=""100"" />
<BootPagination FirstPageText=""|<"" PreviousPageText=""<<"" NextPageText="">>"" LastPageText="">|"" CurrentPage=""10"" TotalCount=""100"" />
```
")
    </CodeTemplate>
</PresentationPart>
<PresentationPart Title="分页大小">
    <Description>
        设置 <code>Size</code> 来设置分页的尺寸大小。
    </Description>
    <RunTemplate>
        <BootPagination Size="Size.SM" TotalCount="30" />
        <BootPagination Size="Size.Default" TotalCount="30" />
        <BootPagination Size="Size.LG" TotalCount="30" />
    </RunTemplate>
    <CodeTemplate>
        @Code.GetCode(@"
<BootPagination Size=""Size.SM"" TotalCount=""30"" />
<BootPagination Size=""Size.Default"" TotalCount=""30"" />
<BootPagination Size=""Size.LG"" TotalCount=""30"" />
")
    </CodeTemplate>
</PresentationPart>
<PresentationPart Title="隐藏或禁用">
    <Description>
        设置 <code>HideOrDisable</code> 若当前页是第一页或最后一页时，“首页/第一页”或“末页/下一页”隐藏（<code>true</code>）或（<code>false</code>）的方式呈现。
    </Description>
    <RunTemplate>
        <h6>禁用效果</h6>
        <BootPagination TotalCount="100" HideOrDisable="false" />
        <BootPagination TotalCount="100" HideOrDisable="false" CurrentPage="10" />
        <h6>隐藏效果</h6>
        <BootPagination TotalCount="100" HideOrDisable="true" />
        <BootPagination TotalCount="100" HideOrDisable="true" CurrentPage="10" />        
    </RunTemplate>
    <CodeTemplate>
        @Code.GetCode(@"
```html
<h6>禁用效果</h6>
<BootPagination TotalCount=""100"" HideOrDisable=""false"" />
<BootPagination TotalCount=""100"" HideOrDisable=""false"" CurrentPage=""10"" />
<h6>隐藏效果</h6>
<BootPagination TotalCount=""100"" HideOrDisable=""true"" />
<BootPagination TotalCount=""100"" HideOrDisable=""true"" CurrentPage=""10"" />    
```
")
    </CodeTemplate>
</PresentationPart>

<PresentationPart Title="隐藏“状态标签”和“跳转到”">
    <Description>
        设置 <code>ShowNavigateTo="false"</code> 隐藏“跳转到”面板。设置 <code>ShowStatusLabel="false"</code> 隐藏分页状态标签。
    </Description>
    <RunTemplate>
        <BootPagination TotalCount="50" ShowNavigateTo="false"/>
        <BootPagination TotalCount="50" ShowStatusLabel="false"/>
    </RunTemplate>
    <CodeTemplate>
        @Code.GetCode(@"
```html
<BootPagination TotalCount=""50"" ShowNavigateTo=""false""/>
<BootPagination TotalCount=""50"" ShowStatusLabel=""false""/>
```
")
    </CodeTemplate>
</PresentationPart>
<PresentationPart Title="隐藏分页数字">
    <Description>
        设置 <code>ShowPageNumber="false"</code> 隐藏分页的数字。
    </Description>
    <RunTemplate>
        <BootPagination TotalCount="50" ShowPageNumber="false" />
    </RunTemplate>
    <CodeTemplate>
        @Code.GetCode(@"
```html
<BootPagination TotalCount=""50"" ShowPageNumber=""false""/>
```
")
    </CodeTemplate>
</PresentationPart>
<PresentationPart Title="设置分页数字的个数">
    <Description>
        设置 <code>PageNumberCount</code> 自定义需要显示的分页数字的个数，默认是 5 个。
    </Description>
    <RunTemplate>
        <BootPagination TotalCount="50" PageNumberCount="15" />
        <BootPagination TotalCount="1000" PageNumberCount="3" />
    </RunTemplate>
    <CodeTemplate>
        @Code.GetCode(@"
```html
<BootPagination TotalCount=""50"" PageNumberCount=""15"" />
<BootPagination TotalCount=""1000"" PageNumberCount=""3"" />
```
")
    </CodeTemplate>
</PresentationPart>

<PresentationPart Title="页码改变事件">
    <Description>
        设置 <code>OnPageChanged</code> 来触发页码改变事件。事件的参数是新页码。
    </Description>
    <RunTemplate>
        <BootPagination TotalCount="100" OnPageChanged="@(page=>_js.InvokeVoidAsync("alert",$"当前第{page}页"))"/>
    </RunTemplate>
    <CodeTemplate>
        @Code.GetCode(@"
```html
<BootPagination TotalCount=""100"" OnPageChanged=""@(page => _js.InvokeVoidAsync(""alert"", $""当前第{page}页""))""/>
```
")
    </CodeTemplate>
</PresentationPart>

@inject IJSRuntime _js

<ArgumentDescriptionTable Title="属性">
    <tr>
        <td>CurrentPage</td>
        <td>Int32</td>
        <td>设置当前页码，必须大于 0。默认是 1。</td>
    </tr>
    <tr>
        <td>TotalCount</td>
        <td>Int32</td>
        <td>设置数据的总记录数。</td>
    </tr>
    <tr>
        <td>FirstPageText</td>
        <td>String</td>
        <td>设置“首页”的文本。</td>
    </tr>
    <tr>
        <td>PreviousPageText</td>
        <td>String</td>
        <td>设置“上一页”的文本。</td>
    </tr>
    <tr>
        <td>NextPageText</td>
        <td>String</td>
        <td>设置“下一页”的文本。</td>
    </tr>
    <tr>
        <td>LastPageText</td>
        <td>String</td>
        <td>设置“最后一页”的文本。</td>
    </tr>
    <tr>
        <td>HideOrDisable</td>
        <td>Boolean</td>
        <td>设置一个布尔值，表示当页数在第1页或最后1页时，使用隐藏或禁用的方式呈现“首页/上一页”或“末页/下一页”。<code>true</code> 表示隐藏，否则为 <code>false</code>。默认是 <code>false</code>。</td>
    </tr>
    <tr>
        <td>Size</td>
        <td>Size</td>
        <td>设置分页的尺寸。</td>
    </tr>
    <tr>
        <td>ShowStatusLabel</td>
        <td>Boolean</td>
        <td>设置一个布尔值，表示是否显示分页的状态标签，例如：“当前 1/4 页 共 233 条记录”，默认是 <code>true</code>。</td>
    </tr>
    <tr>
        <td>ShowNavigateTo</td>
        <td>Boolean</td>
        <td>设置一个布尔值，表示是否显示“跳转到”的组件。默认是 <code>true</code>。</td>
    </tr>
    <tr>
        <td>ShowPageNumber</td>
        <td>Boolean</td>
        <td>设置一个布尔值，表示是否显示分页数。默认是 <code>true</code>。</td>
    </tr>
    <tr>
        <td>PageNumberCount</td>
        <td>Int32</td>
        <td>设置分页页码的个数，默认是 5 个。</td>
    </tr>
    <tr>
        <td>StatusLabelTemplate</td>
        <td>RenderFragment&lt;BootPagination></td>
        <td>设置分页统计标签的自定义内容模板。</td>
    </tr>
    <tr>
        <td>NavigateToTemplate</td>
        <td>RenderFragment&lt;BootPagination></td>
        <td>设置“跳转到”的自定义内容模板。</td>
    </tr>
</ArgumentDescriptionTable>
<ArgumentDescriptionTable Title="方法">

    <tr>
        <td>NavigateToFirst</td>
        <td>Task</td>
        <td>导航到首页。</td>
    </tr>
    <tr>
        <td>NavigateToPrevious</td>
        <td>Task</td>
        <td>导航到上一页。</td>
    </tr>
    <tr>
        <td>NavigateToNext</td>
        <td>Task</td>
        <td>导航到下一页。</td>
    </tr>
    <tr>
        <td>NavigateToLast</td>
        <td>Task</td>
        <td>导航到末页。</td>
    </tr>
    <tr>
        <td>NavigateToPage</td>
        <td>Task</td>
        <td>导航到自定义页。</td>
    </tr>
    <tr>
        <td>ComputePageNumber</td>
        <td>Tuple&lt;int, int></td>
        <td>计算分页数字并返回开始和结束的索引。</td>
    </tr>
</ArgumentDescriptionTable>
<ArgumentDescriptionTable Title="事件">
    <tr>
        <td>OnPageChanged</td>
        <td>EventCallback&lt;int></td>
        <td>设置当页码发生改变时触发的事件。</td>
    </tr>
</ArgumentDescriptionTable>